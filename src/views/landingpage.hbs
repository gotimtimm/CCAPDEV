<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <title>Flight Search</title>

  <style>

    body {
      font-family: Arial, sans-serif;
      background: url("/images/elnido.jpg");
      background-size: cover;
      margin: 0;
      padding: 0px;
    }
    
    h1 {
      color: rgb(255, 255, 255);
      margin-bottom: 50px;
      margin-top: 50px;
      letter-spacing: 2px;
      word-spacing: 10px;
      -webkit-text-stroke: 2px rgb(2, 74, 17);
      font-weight: 800;
    }

    .container {
      max-width: 1250px;
      margin: 0 auto;
      margin-bottom: 0px;
    }

    .results {
      margin-top: 25px;
      background: #ffffffdd;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .flights-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* Responsive grid */
      gap: 15px;
    }

    .flight-card {
      background: #fff;
      opacity: 0.95;
      padding: 15px;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.08);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .flight {
      font-weight: bold;
    }

    .flight-info {
      line-height: 1.4;
    }

    .flight-card-right {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
    }

    .price {
      font-size: 18px;
      font-weight: bold;
      color: red;
      margin-bottom: 10px;
    }

    .book-btn {
      padding: 8px 12px;
      background: #28a745;
      color: white;
      border: none;
      border-radius: 8px;
      text-decoration: none;
      cursor: pointer;
      font-size: 14px;
    }

    .book-btn:hover {
      background: #218838;
    }

  </style>
</head>

<body>

  <div class="container">
    <h1>LA SALLE AIRPORT ✈️</h1>
    <form class="row g-3 bg-light pt-1 px-3 pb-3 rounded" action="/" method="get">
      <div class="col-md-2">
        <select name="origin" class="form-select" required>
          <option value="" disabled selected>Select Origin</option>
          {{#each locations}}
            <option value="{{this}}" {{#if (eq this query.origin)}}selected{{/if}}>{{this}}</option>
          {{/each}}
        </select>
 
      </div>
      <div class="col-md-2">
        <select name="destination" class="form-select" required>
          <option value="" disabled selected>Select Destination</option>
          {{#each locations}}
            <option value="{{this}}" {{#if (eq this query.destination)}}selected{{/if}}>{{this}}</option>
          {{/each}}
        </select>
      </div>
      <div class="col-md-3">
        
        <label for="departure_date" class="form-label visually-hidden">Departure Date</label>
        <input type="date" name="departure_date" class="form-control" value="{{query.departure_date}}" required>
    
      </div>
      <div class="col-md-3">
        <label for="arrival_date" class="form-label visually-hidden">Arrival Date</label>
        <input type="date" name="arrival_date" class="form-control" value="{{query.arrival_date}}" required>
      </div>
      <div class="col-md-1">
        <label for="pax" class="form-label visually-hidden">Passengers</label>
        <input type="number" name="pax" class="form-control" min="1" value="{{query.pax}}" placeholder="Pax">
      </div>
      <div class="col-md-1">
        <button type="submit" class="btn btn-primary w-100 h-100">Search</button>
      </div>
    </form>


    <div class="results">
      <h4 class="mb-4">Available Flights</h4>
      <div class="flights-grid">
        
     
       {{#if flights}}
          {{#each flights}}
            <div class="flight-card">
              <div class="flight-info">
                <p class="flight">{{this.origin}} → {{this.destination}}</p>
                <p>Airline: {{this.airlineName}}</p>
                <p>Flight No: {{this.flightNumber}}</p>
   
                <p>Departure: {{this.departureDate}} @ {{this.departureTime}}</p>
                <p>Arrival: {{this.arrivalDate}} @ {{this.arrivalTime}}</p>
           
                <p>Aircraft: {{this.aircraftType}} ({{this.seatCapacity}} seats)</p>
              </div>
              <div class="flight-card-right">
                <div class="price">₱{{this.basePrice}}</div>
        
                <a href="/reservation-form?flightId={{this._id}}&date={{this.departureDate}}" class="book-btn">Book Now</a>
              </div>
            </div>
 
          {{/each}}
        {{else}}
          <p class="text-center w-100">No flights found.
          Please check back later or use the admin panel to add flights.</p>
        {{/if}}

      </div>
    </div>
  </div>
</body>
</html>