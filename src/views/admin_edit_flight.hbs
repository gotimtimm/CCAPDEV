<!DOCTYPE html>
<html>
<head>
  <title>Edit Flight</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-4">
  <h3>Edit Flight: {{flight.flightNumber}}</h3>

  <form action="/flights/update/{{flight.id}}" method="POST" class="row g-3 bg-light p-3 rounded mb-4">
    <div class="col-md-2">
      <label>Flight No</label>
      <input type="text" name="flightNumber" class="form-control" value="{{flight.flightNumber}}" required>
    </div>
    <div class="col-md-2">
      <label>Origin</label>
      <input type="text" name="origin" class="form-control" value="{{flight.origin}}" required>
    </div>
    <div class="col-md-2">
      <label>Destination</label>
      <input type="text" name="destination" class="form-control" value="{{flight.destination}}" required>
    </div>
    <div class="col-md-3">
      <label>Schedule</label>
      <input type="datetime-local" name="schedule" class="form-control" value="{{flight.schedule}}" required>
    </div>
    <div class="col-md-2">
      <label>Capacity</label>
      <input type="number" name="seatCapacity" class="form-control" value="{{flight.seatCapacity}}" min="50" required>
    </div>
    <div class="col-md-1 d-flex align-items-end">
      <button type="submit" class="btn btn-success w-100">Update</button>
    </div>
  </form>
  
  <a href="/flights" class="btn btn-secondary">Cancel</a>
</div>
</body>
</html>